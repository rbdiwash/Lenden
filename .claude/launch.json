{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "lenden-web",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["expo", "start", "--web", "--port", "8081"],
      "port": 8081,
      "comment": "8081 is React Native's packager port — phones running Expo Go connect to it directly, so it must not be reassigned.",
      "autoPort": false
    }
  ]
}
